# html_extractor
**Author:** davidkhala
**Type:** tool


# Build
requirements.txt is for dify plugin package only. It it generated from [pyproject.toml](pyproject.toml)
uv pip compile pyproject.toml -o requirements.txt
